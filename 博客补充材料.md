# 博客补充材料（可直接粘进你的作业博客）

> 你的博客已经有：作业信息表 / 第一步 AIGC 拆解 / 第二步 四个开发阶段 + 提示词截图 / 第三步 测试表 / 第四步 六个问题。
> 下面补的是**还缺的三块**（PSP 表、心得体会、AIGC 使用过程汇总表），以及**要改的事实错误**。
> 学号已按 102401531 填好。

---

## 一、补：AIGC 使用过程（汇总表）

> 放在「第四步：遇到的问题以及如何解决」之后，作为单独一节。
> 你原来按"阶段 + 提示词截图"的写法很好，保留；这张表是作业要求的**结构化汇总**，两者不冲突。

| 子任务 | 借助何种 AIGC 技术 | AI 实现或提供了什么 | 效果如何 | 人工修改 |
| --- | --- | --- | --- | --- |
| 需求分解与开发顺序 | DeepSeek 编程助手 | 把"做一个一箭又一箭小游戏"拆成界面 / 路径逻辑 / 关卡 / 测试等模块并排顺序 | **拆得太碎，第一版方向跑偏**：做成了"旋转箭头对齐方向"的玩法 | 自己重写核心玩法：点一下到底发生什么、判定范围、两种结果 |
| 核心路径判定 | DeepSeek 编程助手 | 生成方向位移映射与邻接表式判定代码 | 代码能跑，但**"画出来朝上、判定朝左"**，只在边缘暴露 | 收敛成唯一一份 `DIR_STEPS`，判定与绘制共用；加"渲染朝向必须和数据方向一致"的测试 |
| 动画与碰撞反馈 | DeepSeek 编程助手 | 先实现为"本体不动 + 另画一个特效飞出去" | 视觉上像**幻影**：原地留着一支箭，另一支飞出去又回来 | 改成 `Arrow.offset` 驱动**本体**沿自己的轨道运动，判定随之简化成只看箭头那一格 |
| 关卡可解性 | DeepSeek 编程助手 | 提出"随机生成 + 求解器筛选无解"的方案 | 满盘时能飞的箭只有贴边朝外那几支，**随机 6×6 可解率约万分之一**，拒绝采样几乎永远失败 | 改为**反向构造**（按拆除顺序倒着摆），并加"每关必须可解"的自动化测试 |
| 撤销 / 提示 / 音效 / 创意工坊 | DeepSeek 编程助手 | 按需求实现各个拓展功能 | 撤销"只恢复箭头位置"会破坏邻居图；音效在真机上完全静默 | 撤销改"最初那张图 + 操作序列重放"；音效改为先读回 mixer 真实格式再合成 |
| 打包可执行文件 | DeepSeek 编程助手 | 给出 PyInstaller 打包脚本与参数 | `--onefile` 在写不了 `%TEMP%` 的环境下解包失败、进程卡死 | 改 `--onedir`，并新增"让 exe 自己跑一遍自检、看退出码"的验证方式 |

---

## 二、补：个人软件过程（PSP）报告

> 作业要求里有 PSP 表（同课程同学都交了）。下面是**你的真实数据**，可直接粘。
> 如果老师那边要求"小时"口径，用后面的换算版本。

| 阶段 | 预计耗时（分钟） | 实际耗时（分钟） | 差异（实际 − 预计） |
| --- | ---: | ---: | ---: |
| 需求、环境和任务拆分 | 10 | 3 | −7 |
| 规则、关卡与测试；界面并行开发 | 240 | 120 | −120 |
| 集成、自动化验收与画面检查 | 30 | 15 | −15 |
| README、截图和测试记录 | 30 | 20 | −10 |
| **合计** | **310** | **158** | **−152** |

小时口径（约）：**预计 5 小时 10 分 / 实际 2 小时 38 分 / 差异 −2 小时 32 分**，
实际耗时约为预估的 **51%**。

### 差异原因（建议照这段写，直接对应表里的 −120）

四个阶段都低于预估，其中「规则、关卡与测试；界面并行开发」这一项差了 120 分钟，原因有两个：

1. **界面与规则/关卡/测试是并行推进的**，不是串行等前一步做完；
2. **样板代码由 AIGC 出初稿**（状态机骨架、界面绘制、动画、音效合成），我只做核对与修改，
   这部分比手写快得多；关卡可解性改用**反向构造**之后，也避免了我第一版人工摆关卡造成的返工。

-15 和 -10 两项则是因为**自动化验收脚本（`run_tests.py`）在集成之前就写好了**，
集成时一条命令跑完，不用手工逐条点；README 和测试记录又是同一天把真实结论整理下来的，不用回头补。
-7 是任务拆分本身由 AIGC 出初稿。

**也要如实写一句反面**：虽然总耗时比预估少，但**返工发生在预估之外**——
第一版玩法做错（旋转箭头对齐方向）和方向映射 bug 都导致过整块重写；
如果没有这两次返工，实际还会更短。正因为这样，我没有把"AI 省时间"写成一路顺风。

> 另外提醒一处：你博客里有「阶段四 打包成可执行文件」，但 PSP 四行里没有单独列打包。
> 两个处理都行：① 把它并进第 3 行「集成、自动化验收与画面检查」；
> ② 单列一行（把打包那几分钟给我，我更新合计：预计 310+x / 实际 158+y / 差异相应变化）。

---

## 三、补：心得体会

> 建议放在 PSP 表之后、仓库地址之前。下面的稿子可直接用或改。

**AI 帮了什么。** 省时间的主要是样板代码和查错。界面绘制、状态机、动画这些我不太想手写的部分，AI 给得很快；
排查问题也快——比如段错误那个崩溃，堆栈指向 `font.render()`，我一开始完全不知道从哪下手，
AI 直接说是模块级字体缓存跨 `pygame.quit()` 复用导致对象失效。还有反向构造法这个思路是 AI 提的：
我原来的想法是随机摆布局再用求解器筛掉无解的，效率极低（实测随机 6×6 可解率约万分之一），
反向构造从根上保证有解，这个我没想到。

**出了问题的地方。**

* **AI 第一版给的代码不一定对。** 方向映射那次最典型：程序不报错、绝大多数情况看着也正常，只在边缘暴露，
  而且错误是"画出来朝上、判定朝左"这种，光看运行日志根本看不出来，是我盯着截图看出来的。
  这让我意识到，不能因为"跑起来了"就当成对的。
* **我自己摆的关卡也是错的。** 手工摆的关卡里有死局（箭头互相指着形成闭环），肉眼完全看不出来。
  这件事让我明白"把正确性写成自动化测试"不是形式主义，是真的能挡住错误。
* **测试本身也会错。** 我的自检里有一版判据写反了（以为三角形质心偏向箭尖，其实偏向底边），
  结果 33 支箭全报错；还有一次"两支箭重叠"的测试数据其实根本不重叠。断言失败时先怀疑测试。
* **最危险的是"测试全绿、功能全死"。** 音效那次，无窗口测试里一切正常，放到真机上才发现功能是哑的——
  因为 `try/except` 把初始化错误吞掉了。**"能跑起来"和"好使"中间差着一个验证。**
* **AI 不知道界面好不好看。** 文字压到按钮上、动画像幻影，都是我截图给它看之后才改的。

**收获。** 这次最大的收获不是学会 Pygame，而是摸出了和 AI 协作的分寸：
需求要写清"输入是什么、输出是什么、什么算对"；让 AI 写，但关键逻辑自己得看懂、自己验证；
复杂的东西先要一个小版本，再一轮一轮改。作业说助教可能针对关键代码提问，我觉得这个要求挺合理——
代码可以 AI 写，但责任在我自己。

---

## 四、必须改的事实错误（5 处）

| # | 你现在写的 | 应该改成 | 为什么 |
| --- | --- | --- | --- |
| 1 | `可通过 run_test.py 复现` | **`run_tests.py`** | 文件名错了，助教按这个命令跑会找不到 |
| 2 | 阶段四标题 `ArrowAftetArrow` | **`ArrowAfterArrow`** | 拼写错误 |
| 3 | "一个箭头能飞，当且仅当它前进方向上的邻居是 None" | 补一句限定：这是**单格箭头**时的写法；支持多格/拐弯箭头之后，判定点是**箭头所在那一格**，判定时要**沿邻居链跳过自己的箭身**（箭身可能落在自己箭头的射线上，那不是阻挡） | 现在博客的描述和最终代码不一致，助教拿着代码一问就会露馅 |
| 4 | "一个箭头飞走后就地拆链：把它上下两个邻居对接、左右两个邻居对接" | "把它**每个**格子逐个拆链，每个格子都是上下对接、左右对接" | 多格箭头有多个格子，要逐个摘 |
| 5 | 问题二里 "解决方法：改用邻接表存储" | 根因是**判定和绘制各用了一套方向映射**（一边按屏幕坐标 `(dx,dy)` 写、一边按行列增量 `(dr,dc)` 解包，于是 `UP=(0,-1)` 被当成"向左"）；修法是**收敛成唯一一份 `DIR_STEPS`**，并加"渲染出的箭尖朝向必须和数据方向一致"的测试。**邻接表是 O(1) 查询用的结构，不是这个 bug 的根因** | 你现在的因果链是错的；这个 bug 你博客里已经贴了截图，正好是"AI 代码能跑但是错的"最好的例子，把根因说准了更有说服力 |

另外两处小核对：
* 问题四里写的「其余 7 条用例和 20 项自检全绿」→ 自检现在是 **22 项**；
* 测试一节建议补一行运行命令和结果：`python run_tests.py` → `合计：9/9 通过`，以及"另有内部 22 项自检全过"。

---

## 五、还差的两件（发布前必须做）

1. **填仓库链接**。作业信息表里的 `<仓库链接>` 和文末的仓库地址都要换成真实地址。
   建仓库的操作步骤见同目录的 **`PyCharm建仓库与提交步骤.md`**（用 PyCharm 内置 Git，不用另外装 git）。
2. **图片要换成博客园相册的链接**。你现在引用的都是 `img2024.cnblogs.com/blog/3847972/...`，
   说明已经传上去了；把新截的图（如果有补充）也一起传上去再贴。

---

## 六、关卡数：已决定维持 3 关（方案 A）

你上一条问过「扩到 5 关 + 补测试」，最终选择 **A：维持 3 关**。
所以：

* 代码、测试**不动**（内置关卡仍是 3 关：6×6 / 6×6 / 8×8）；
* 你博客里「增加了三个固定关卡」「T03 遍历 3 个关卡共 9 支边缘箭」「T04 加载第 2 关（8 支箭）」
  这些数字**全部正确，不用改**；
* 我原来准备的那张"扩到 5 关后哪里会变"的对照表作废了，不再列在这里，免得以后看着混乱。

如果期末想再加内容，可以直接往 `LEVELS` 里追加关卡（每关就是一个字典），
或者用创意工坊的地图机制当作"更多关卡"的展示——那条路不需要改代码。

---

## 七、再补两处说法不一致（会被助教问到的那种）

上面第四节列了 5 处，这里再补 2 处。这两处都是**"描述和实现不一致"**，
平时看着没事，但助教拿着代码对照着问就会卡住：

| # | 你现在写的 | 应该改成 | 为什么 |
| --- | --- | --- | --- |
| 6 | 阶段三："增加了创意工坊玩家可以上传**自己设计的功能**和地图" | "创意工坊分成两部分：**功能**是内置的提示 / 撤销 / 音效，玩家只能**开关**、不能自己加；**地图**是玩家自己写的 `.json`，放进 `maps/` 目录就算上传" | 功能那一半目前**不是插件系统**（卡片由一张 `FEATURES` 表生成，但行为写死在代码里）。说成"可以上传功能"会被追问"上传入口在哪" |
| 7 | 阶段三："玩家可自己**通关后可上传**自己创作的地图" | "上传**没有门槛**（把文件丢进 `maps/` 就行）；自己试玩**通关过的图会盖上「已通关」标记**（按地图内容指纹记录，改了文件自动失效）" | 现在没有"必须先通关才能上传"的限制。另外可视化地图编辑器我们**评估后决定不做**（工作量约等于整个界面层）——如果助教问"能不能在游戏里画地图"，就这么答，理由很充分 |

> 可选加分点：创意工坊这一段其实可以写成一个小亮点——"工坊的可扩展性分两半：
> 地图是真扩展（文件驱动，玩家不碰代码就能加内容）；功能是配置化而不是插件化，
> 这一点我在 README 里如实写清楚了，没把它包装成插件系统"。诚实说明边界，比含糊地吹更好。
